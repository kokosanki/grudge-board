export interface UserType {
  id: string;
  email: string;
}

export interface PointType {
  id: string;
  name: string;
  text: string;
  created_at: string;
  created_by: string;
  user_id: string;
}